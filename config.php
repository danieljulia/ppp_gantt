<?php
declare(strict_types=1);

// Admin password - change this to your desired password
// This should be hashed in production, but for simplicity we'll use plain text
// You can use password_hash() and password_verify() for better security
return [
    'admin_password' => 'admin123', // Change this!
];

